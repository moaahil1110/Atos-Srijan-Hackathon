# Project Notes

This folder contains internal planning and architecture notes from the build process.

These files are useful for reviewers who want extra implementation context, but they are not the runtime source of truth for retrieval. The actual local retrieval corpus lives in:

- `docs/compliance`
- `docs/providers`
