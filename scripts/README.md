# Scripts

- `build_index.py`
  Rebuilds the compliance, Azure, and GCP FAISS indexes from the tracked local docs.

- `fetch_provider_docs.py`
  Utility script used to fetch and normalize provider documentation into the tracked markdown files.
