# Tests

This folder contains evaluation and smoke-test helpers.

- `run_evaluation.py` runs the end-to-end evaluation flow against a local backend.

Generated logs, raw responses, and evaluation reports are intentionally ignored in Git so the repository stays clean for reviewers.
